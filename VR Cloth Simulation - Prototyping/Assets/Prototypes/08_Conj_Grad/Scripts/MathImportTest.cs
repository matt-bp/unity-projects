using UnityEngine;

namespace Prototypes._08_Conj_Grad.Scripts
{
    public class MathImportTest : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log(MattMath.HelloMath.Print());
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
